# 库存盘点 WebApp 部署方案调研报告

## 项目概况

- 项目位置：`~/sketches/inventory-app/`
- 形态：单文件 HTML SPA（1047行）
- 关键依赖：`html5-qrcode`（扫码）、`unpkg.com` CDN
- 需求：HTTPS（iOS getUserMedia 需要）、中国可访问、免费
- 用户位置：福建宁德

## 网络测试结果

| 服务 | 延迟 | IP归属 | 中国可访问性 |
|------|------|--------|------------|
| Gitee.com | 0.33s | ✅ 中国 (180.76.198.77) | ✅ 极佳 |
| Coding.net | 0.54s | ✅ 中国 (175.24.154.130) | ✅ 极佳 |
| GitHub Pages | 0.57s | ❌ 美国 | ⚠️ 通常可用 |
| raw.githubusercontent.com | 1.03s | ❌ 美国 | ⚠️ 通常可用 |
| cdn.jsdelivr.net | 1.05s | ✅ 有国内CDN节点 | ✅ 良好 |
| pages.cloudflare.com | 2.30s | ❌ Cloudflare | ⚠️ 部分地区可用 |
| Vercel | 超时 | ❌ 美国 | ❌ 不稳定 |
| Surge.sh | ❌ | ❌ 美国 | ❌ 常被屏蔽 |
| workers.dev | - | - | ❌ 前方案已确认被屏蔽 |

---

## 方案一（强烈推荐）：Gitee Pages（码云静态页）

**最适合中国的方案。国内访问速度最快，无需翻墙。**

### 步骤

#### 1. 注册 Gitee 账号
- 打开 https://gitee.com 注册
- 已有账号则直接登录

#### 2. 创建仓库
- 点「+」→ 新建仓库
- 仓库名：`inventory-app`（或其他）
- 选择「公开」
- 勾选「使用 Readme 文件初始化」

#### 3. 上传 index.html
- 进入仓库
- 点「文件」→「上传文件」
- 选择 `index.html` 上传
- 或使用命令行：
```bash
git clone https://gitee.com/你的用户名/inventory-app.git
cd inventory-app
cp ~/sketches/inventory-app/index.html .
git add index.html
git commit -m "添加库存盘点App"
git push
```

#### 4. 开启 Pages 服务
- 进入仓库 →「服务」→「Gitee Pages」
- 选择部署分支：`master`（或 `main`）
- 目录：`/`
- 点「启动」
- HTTPS 勾选「强制 HTTPS」
- 等待几分钟

#### 5. 访问
- 获得链接：`https://你的用户名.gitee.io/inventory-app/`
- iPhone 扫码即可打开

### 注意事项
- 仓库必须为**公开**（免费）
- 更新后需手动点「更新」同步 Pages
- Gitee Pages 强制 HTTPS

---

## 方案二（备选）：GitHub Pages

**如果已有 GitHub 账号，最简单快捷。**

### 步骤

#### 1. 创建 GitHub 仓库
- 登录 https://github.com
- 新建仓库 `inventory-app`（公开）
- 不勾选初始化文件（稍后推送）

#### 2. 推送代码
```bash
cd ~/sketches/inventory-app
git init
git add index.html
git commit -m "初始化库存盘点App"
git remote add origin https://github.com/你的用户名/inventory-app.git
git push -u origin main
```

#### 3. 开启 Pages
- 仓库 → Settings → Pages
- Source: Deploy from a branch
- Branch: `main`, folder: `/`
- Save
- 等待 ~1分钟

#### 4. 访问
- `https://你的用户名.github.io/inventory-app/`

---

## 方案三（备选）：Cloudflare Pages

**速度快，全球 CDN，但需要 Cloudflare 账号。**

### 步骤
1. 登录 https://dash.cloudflare.com
2. Workers & Pages → Pages → 创建项目
3. 连接 Git 仓库（GitHub/GitLab）
4. 框架预设：None
5. 构建命令：留空
6. 构建输出目录：`/`
7. 部署后获得 `https://项目名.pages.dev`

---

## 方案四（临时方案）：Cloudflare Tunnel + 本地服务器

**无需任何账号，但电脑必须开机。适合临时测试。**

### 步骤
1. 启动本地 HTTP 服务器：
```bash
cd ~/sketches/inventory-app
npx serve -l 8000
```

2. 安装并启动 Cloudflare Tunnel：
```bash
# 在另一个终端
npx cloudflared tunnel --url http://localhost:8000
```

3. 获得 `https://xxxxx.trycloudflare.com` 链接

---

## 方案五（国内CDN加速）：jsDelivr + GitHub

**利用 jsDelivr 在国内的 CDN 节点，访问速度极快。**
但注意：**SPA 只有一个页面，没有路由问题，其实很适合。**

### 步骤
1. 按方案二推送到 GitHub
2. 创建一个 GitHub Release（打 tag）
3. 访问：
   `https://cdn.jsdelivr.net/gh/你的用户名/inventory-app@v1.0.0/index.html`
4. 每次更新需要新 Release

---

## 配套优化：替换 CDN 资源

原代码从 `unpkg.com` 加载 html5-qrcode，该域在中国可能很慢。
建议替换为国内 CDN：

```html
<!-- 原版（unpkg，可能慢） -->
<!-- <script src="https://unpkg.com/html5-qrcode@2.3.8/html5-qrcode.min.js"></script> -->

<!-- 方案A：cdnjs（Cloudflare 国内节点） -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/html5-qrcode/2.3.8/html5-qrcode.min.js"></script>

<!-- 方案B：cdn.bootcdn.net（中国 CDN，最快） -->
<!-- 先查是否有 html5-qrcode：https://www.bootcdn.cn/ -->
```

但实际上 `unpkg.com` 在 3.94s 可访问，且 Cloudflare 的国内速度还行。
如果最终是用 Gitee Pages（国内服务器），则 CDN 加载速度影响不大。

---

## 方案对比总结

| 方案 | 国内速度 | 配置难度 | HTTPS | 需要账号 | 持续运行 |
|------|---------|---------|-------|---------|---------|
| ① Gitee Pages | 🚀 极快 | 低 | ✅ 自动 | Gitee账号 | ✅ |
| ② GitHub Pages | ⚠️ 通常可 | 低 | ✅ 自动 | GitHub账号 | ✅ |
| ③ Cloudflare Pages | ⚠️ 部分可 | 中 | ✅ 自动 | CF账号 | ✅ |
| ④ CF Tunnel | ⚠️ 部分可 | 中 | ✅ 自动 | 无 | ❌ 需电脑开机 |
| ⑤ jsDelivr | 🚀 极快 | 中 | ✅ 自动 | GitHub账号 | ✅ |

## 最终推荐

**首选 Gitee Pages**（方案一）：
- 中国境内访问最快
- 配置最简单（全网页操作）
- HTTPS 自动开启
- 免费
- 与中国用户网络环境完全兼容

**如果已有 GitHub 且不想注册 Gitee**：
- 使用 GitHub Pages（方案二）即可
- 福建地区访问 GitHub Pages 通常没问题
